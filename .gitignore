# --- secrets: never commit these -------------------------------------------
.env
.env.*
*.pem
*.key
secrets.json
supabase/.temp/
**/service_role*
**/.netrc
**/local_secrets.dart

# --- local Postgres test artefacts ------------------------------------------
/tmp/
*.log
pgdata/
terrastep_pg/

# --- Flutter / Dart (for when Phase 1 lands here) ---------------------------
.dart_tool/
.packages
.pub-cache/
.pub/
build/
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
android/.gradle/
android/local.properties
android/key.properties
*.iml
*.jks
*.keystore

# --- editors / OS -----------------------------------------------------------
.idea/
.vscode/
.DS_Store
Thumbs.db
*.swp

# --- Flutter generated platform folders (regenerated by CI) -----------------
app/android/
app/ios/
app/linux/
app/macos/
app/windows/
app/web/
app/.dart_tool/
app/build/
app/pubspec.lock

# CI / local build output. The *deliverable* APK is committed under
# releases/ so it can be downloaded from the repo on a phone (#27).
dist/
